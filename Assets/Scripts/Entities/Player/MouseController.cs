using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MouseController : MonoBehaviour
{
    private void Start()
    {
        // TODO: Look into getting the interactables only once instead, saving performance.
    }

    private void Update()
    {
        var nothing = 0;
        
        // Cast a ray.
        
        // Check if the hit object implements the IInteractable interface.
        
        // If true, Check if the player pressed the mouse button.
        
        // If they did, call OnClick on the hovered object, and OnHover if they didn't.
    }
}
